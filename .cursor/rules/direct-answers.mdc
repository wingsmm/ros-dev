---
description: 有问题直接答根因，不绕弯、不反复试探、不用模棱两可表述
alwaysApply: true
---

# 直接回答问题

## 原则

用户问「什么问题」「什么情况」「是不是 X」时：

1. **第一句就给结论**（根因 / 责任方 / 当前状态），不要铺垫长篇背景。
2. **说清是什么**，不要堆「不是 A 而是 B」「可能 / 或许 / 更像是」——除非事实真未确认，且要写明**还差哪一条证据**。
3. **能一句话说清的不要拆成表格 + 流程 + 建议三连**；用户没要方案时，先答问题本身。

## 禁止

- 用「不是 Qt 坏了，是机器人没发布」这种绕弯句式——应直接说：**机器人端没发布 depth preview**。
- 没查清就连续 deploy / restart / 多轮 SSH 碰运气；**先只读诊断，根因明确再动**。
- 用「建议你 / 可以考虑 / 下一步可以」代替直接答案。
- 把已做过的检查再复述一遍当回复主体。

## 诊断与动手

| 用户意图 | 做法 |
|----------|------|
| 问原因 / 什么情况 | 只读检查 → **一句根因** → 必要时 1 条修复命令 |
| 明确要求修 / 部署 | 再执行变更 |

## 示例

```
用户：深度为什么看不到？

❌ 不是 UI 卡死，而是数据链路两端没接上；camera_raw 下 preview 关着……

✅ 机器人 `camera_raw` 没发 `/camera/depth/preview`，PC 也没有 depth bridge，所以 Qt 收不到深度流。
   要看图：`PROFILE=camera_preview` restart。
```

```
用户：是脚本问题还是改错了？

❌ 两边都有道理，Qt 方向对，脚本也可能有问题……

✅ 当前是机器人端发布缺失（preview 未开）；Qt 双 fallback 逻辑没问题。`camera_preview` 启动失败另查 web_video.log。
```
