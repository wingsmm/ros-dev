# 2026-07-06 开发记录

## 今日任务

- VMware Qt「雷达/里程计」RViz 配置已更新：
  - `vmware/qt/config/rviz_mapping.rviz`
  - `Odometry` 显示默认打开：`Enabled: true`、`Value: true`
  - `Topic: /odom` 保持不变
  - `Map` 继续默认关闭

- VMware Qt README 已同步：
  - `vmware/qt/README.md`
  - 「雷达/里程计」说明更新为查看 `/scan`、`/odom`、TF（Map 默认关）

- 已部署到 VMware VM `192.168.1.154`：
  - 使用 `vmware/scripts/vm_qt_remote.bat deploy`
  - 远端路径：`/home/xtark/ros-dev/vmware/qt`
  - 远端 PyQt5 / `run.sh` 状态检查通过

- 真机 `192.168.1.168` 与 VM `192.168.1.154` 联调：
  - `/scan`、TF、`/odom` 在 RViz 中可见
  - Qt 选择「雷达/里程计」启动 RViz 后，`/odom` 箭头默认显示
  - 小车运动后 RViz 中可观察到移动联动

- 修复 VMware Qt teleop bridge 持续发布问题：
  - `vmware/qt/core/teleop_publisher.py`
  - `vmware/qt/scripts/teleop_ros1_bridge.py`
  - bridge 改为按 `TELEOP_REPEAT_HZ` 持续发布最近速度
  - 增加 watchdog，短时间未收到新速度时自动发布零速，保留 dead-man 语义
  - 修复后验证：Qt 五键遥控可驱动小车运动

## 验收结论

- RViz `/odom` 默认显示：通过
- `Map` 默认关闭：通过
- VM 154 连接真机 ROS Master：通过
- `/cmd_vel` 发布到 `/xtark_driver`：通过
- 小车实际运动与 RViz 移动显示联动：通过
