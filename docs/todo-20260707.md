# 2026-07-07 开发记录

## 今日任务

- 首次拉起 Jetson 82 (`nvidia@172.0.0.82`) `~/qt/ros2_ws` 开发链路：
  - `jetson/scripts/jetson.sh probe` 探明 Ubuntu 22.04 + ROS Humble
  - `jetson/scripts/jetson.sh ros2 deploy` 首次编译 `cmd_vel_car_web_bridge`
    （colcon 2.01s 通过，install 落到 `~/qt/ros2_ws/install/`）
  - `jetson/scripts/jetson.sh ros2 verify` Jetson 本机 DDS 干跑五动作全命中

- 打通 PC/WSL cockpit ↔ Jetson bridge 跨机 DDS：
  - WSL `ROS_DOMAIN_ID=0` 直接 `ros2 topic info` 看到 `/cmd_vel` sub=1、
    `/vehicle/control_action` pub=1
  - `jetson/cockpit/scripts/cmd_vel_sender.py` 从 WSL 依次发五动作，
    Jetson bridge 日志逐条打出 `CONTROL_ACTION=FORWARD/BACKWARD/
    TURN_LEFT/TURN_RIGHT/STOP`
  - Qt cockpit 五键遥控 + 键盘 W/A/S/D/Space 实机点了一遍，
    `jetson/cockpit/logs/jetson-cockpit-2026-07-07.log` 里同秒对齐
    「发布 /cmd_vel → 收到 CONTROL_ACTION」

- Git commit `bd8bec6`：feat(jetson) 打通 cockpit ↔ Jetson ros2_ws 干跑控制链路
  （30 files, +2004/-101）
  - Jetson 侧 ROS2 包 `cmd_vel_car_web_bridge` 首次入库
  - `bridge_stack.sh`：nohup + PID 文件的 bridge 生命周期入口
  - cockpit Qt 上位机 + `cmd_vel_sender` / `control_action_echo` CLI
  - `jetson.sh` 扩展 `ros2 push/deploy/build/start/stop/status/logs/verify`
  - `.gitignore` 白名单 `jetson/cockpit/.env` + `logs/.gitkeep`

- 只读探测 `~/qt/car_web`（不动它）：
  - stdlib `HTTPServer` @ 8080，已在跑（pid 10492）
  - 路由 `/api/rear_drive`、`/api/rear_stop`、`/api/steer_motor_run` 等确认存在
  - 结论：cockpit → bridge → car_web HTTP 对接可行，方案见
    `jetson/docs/cmd_vel-car_web-对接方案.md`

## 验收结论

- Jetson `~/qt/ros2_ws` 首次 colcon build：通过
- Jetson 本机 DDS 干跑五动作闭环：通过
- WSL 跨机 DDS discovery：通过
- WSL → Jetson 跨机 `/cmd_vel` 五动作命中：通过
- Qt cockpit 遥控实机操作：通过
- `ros2 stop` 无 orphan 收尾：通过
