# 2026-07-08 开发记录

## 今日任务

### A. VMware Qt + 真机 `pc_stack`「雷达/里程计」最小闭环

- 真机 `192.168.1.168` 使用 `pc_stack camera-start` 拉起基础栈，确认 Master 可达。
- 只读检查：`/scan`、`/odom`、`/cmd_vel`、TF（`odom -> base_footprint -> laser`）齐全。
- 验收口径：RViz Fixed Frame=`odom`；能见障碍物；五键 `/cmd_vel` 推动车体时 odom/scan 联动。
- 文档：`vmware/docs/VMware Qt雷达里程计联调指南.md`（并更新 `vmware` 索引）。

### B. Jetson L1 全链路尝试 → 发现方向错 → 回退

目标本想对齐宇树 L1：`cloud+imu → Point-LIO → /odom`，并接到 cockpit/RViz。链路基本打通，但整体方向搞错了（不该在现阶段自造整栈 / 复杂入口），需要回到远端干净原版。

处置：

| 位置 | 状态 |
|------|------|
| 远端 Jetson `~/qt/ros2_ws` | 已删掉改过版本，从 `~/ros2_ws` 拷回**原始版本**（无我们自写的 `scripts/`、adapter、bridge 包） |
| 本地改过的完整链路代码 | 已迁到仓库根 `other/ros2_ws/`（`.gitignore` 已忽略 `other/`，不入库） |
| 本地 `jetson/mirror/ros2_ws` | 已 `pull` 自远端原版（仅 `src/`：unitree + point_lio + 示例包，无 `scripts/`） |
| Jetson 主机 | 当前可达 IP 为 `172.0.0.52`（`jetson/scripts/.env` 的 `HOST`）；旧 `.82` 不可达 |
| 文档 | `jetson/README.md`、`l1-lio-drift-triage-task.md`、本文件已标明「撤回 + 归档」 |

归档包里的内容（仅作存档，**不是当前工作区入口**）：

```text
other/ros2_ws/
  scripts/   ros2_stack / unilidar_stack / lio_stack / bridge_stack
  src/       cmd_vel_car_web_bridge, lio_odom_adapter, base_odom_adapter,
             unitree_lidar_*, point_lio_ros2
```

相关结论已记入：

- `jetson/docs/l1-lio-drift-triage-task.md`（漂移排查；保留作后续参考，当前不作为日常启动依据）
- `jetson/cockpit/README.md`（L1 卧放 / Z 轴朝前的坐标说明）

## 验收结论

- VMware Qt ROS1「雷达/里程计」口径已文档化，可复用。
- Jetson ROS2 L1 整栈自研方向撤回：工作区 mirror 回到远端原版；自研完整链路仅保留在 `other/ros2_ws`。

## 遗留

- 是否从 `other/ros2_ws` 抽「最小官方 demo 包装」（不自造 LIO/adapter）另议。
- cockpit 侧围绕 L1/RViz 的未提交改动需再筛：保留文档与只读观测，丢掉绑死自研 `ros2_stack` 的入口（若有）。
- Jetson IP 以 `.env` 为准；文档里写死的 `172.0.0.82` 择机统一改成「以 `.env` HOST 为准」。
