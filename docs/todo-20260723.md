# 2026-07-23 近场深度阶段 B：数据闭环

## 目标

```text
B1：168 本机安全录制原始深度 bag
B2：154 本地 Master(11321) 隔离回放
B2 cloud：过滤 bag 相机 TF + nominal YAML + /vmware/depth/points
```

不做障碍/落差/楼梯/导航/Qt 按钮/物理标定。

## 入口

| 侧 | 命令 |
|----|------|
| 168 部署 | `xtark\scripts\pc_stack_remote.bat deploy`（含 `astra_capture.sh`） |
| 154 部署 | `vmware\scripts\vm_qt_remote.bat astra-deploy`（不影响默认 Qt `deploy`） |
| 录包 | `~/ros_ws/scripts/astra_capture.sh start --scene ... --lighting ...` |
| 回放 | `~/ros_ws/devel/lib/astra_nearfield_ros1/astra_replay.sh {check,raw,cloud,stop}` |

## 终审结论（2026-07-24）

```text
STAGE B DATA CLOSED-LOOP AUDIT PASS
EXTRINSICS = nominal（沿用阶段 A）
BASE FRAME = base_footprint
POINT CLOUD = /vmware/depth/points
ISOLATED MASTER = 127.0.0.1:11321
```

| 提交 | 说明 |
|------|------|
| `7d576ad` | 基线：capture + isolated replay（演示通过） |
| 本提交 | 审计修复：lifecycle / exit codes / conflict status |

### 审计复验证据（2026-07-24）

| 项 | 结果 |
|----|------|
| 154 cloud×2 @0.25（原 bag） | depth=1126 cloud=1125；`tf_guard_status_received=true`；`conflict=false` |
| 154 `astra_replay.sh stop` | `PORT_11321_CLOSED`；无自有回放进程残留 |
| 168 10s 定时录制 | `audit_fix_10s`；`exit_status=ok`；`rosbag_exit_code=0` |
| 168 手动 stop | `STOP_TEST_PASS`；bag 落盘（~88MB）；`exit=stopped` |
| TF filter 单测 | 8/8 PASS |

### 已关闭的审计项

1. 回放结束/异常默认自动清理；`--keep-alive` 才保留
2. `tf_guard_conflict` 来自 `/astra_camera_tf_guard/status`（未收到前为 null）
3. rosbag record/play 真实退出码 + bag/报告计数校验
4. stop 前 `/proc/<pid>/cmdline` 身份校验
5. 磁盘预算随 duration/RGB 估算
6. 默认 rate=0.25；文档与 `astra-deploy` 对齐
7. stop 后证明 `:11321` 与回放节点退出（含 orphan `rosmaster`）
8. 回放前检测在线近场；`ASTRA_REPLAY_ALLOW_FOREIGN=1` 可覆盖
9. 跨 shell 手动 stop：不 `wait` 非 child；等 `.bag.active` 落成 `.bag`

## 明确不做

障碍/落差/楼梯、costmap、导航、Qt 按钮、物理外参 measured、Jetson ROS2。
