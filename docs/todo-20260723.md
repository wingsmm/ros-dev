# 2026-07-23 近场深度阶段 B：数据闭环

## 目标

```text
B1：168 本机安全录制原始深度 bag
B2：154 本地 Master(11321) 隔离回放
B2 cloud：过滤 bag 相机 TF + nominal YAML + /vmware/depth/points
```

不做障碍/落差/楼梯/导航/Qt 按钮/物理标定。

## 入口

| 侧 | 命令 |
|----|------|
| 168 部署 | `xtark\scripts\pc_stack_remote.bat deploy`（含 `astra_capture.sh`） |
| 154 部署 | `vmware\scripts\vm_qt_remote.bat astra-deploy`（不影响默认 Qt `deploy`） |
| 录包 | `~/ros_ws/scripts/astra_capture.sh start --scene ... --lighting ...` |
| 回放 | `~/ros_ws/devel/lib/astra_nearfield_ros1/astra_replay.sh {check,raw,cloud,stop}` |

## 验收口径

- 同一 bag，不连在线机器人 Master
- raw：深度/CameraInfo 可读
- cloud：稳定发布 `/vmware/depth/points`；两次回放统计一致
- 真机 nearfield bag 的目标 TF **删除数允许为 0**（本 bag 实测 dropped=2，来自 `/tf_static`）
- 合成单测必须能删除两条目标边；真机回放无 TF guard 冲突；guard 独占 nominal 相机边

## 自测记录（2026-07-23）

```text
STATUS: RUNTIME VALIDATED (168 capture + 154 isolated replay)
VALIDATED_RATE: 0.25
BAG: ~/xtark_logs/astra_nearfield/bags/20260723_162913_flat_floor/capture.bag
```

| 项 | 结果 |
|----|------|
| 本地 `bash -n` + TF filter 8 单测 | PASS |
| 168 `flat_floor` 60s stationary | PASS，663MB，depth/info 1126 |
| 154 `check` / `raw` | PASS，depth=1126，Master=11321 |
| 154 `cloud`×2 @0.25 | PASS，depth=1126 cloud=1125 两次一致，dropped=2，无 conflict |
| `cloud` @0.5 | 不稳定（1126 vs 1125），已降速 |
